# Define a function to calculate the area of a rectangle
def calculate_area(length, width):
    area = length * width
    return area

# Test the function with sample lengths and widths
length1, width1 = 5, 3
area1 = calculate_area(length1, width1)
print(area1)  # Output: 15
